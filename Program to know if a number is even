#include <stdio.h>

#include <stdlib.h>

#include <math.h>

int main() {
  int n;
  printf("Input the number:\n");
  scanf("%i", & n);
  if (n % 2 == 0) {
    printf("%i is an even\n", n);
  } else {
    printf("%i is an odd\n", n);
  }
  system("pause");
  return 0;
}
